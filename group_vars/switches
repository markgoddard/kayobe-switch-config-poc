---
# Global switch config.
switch_config: "{{ lookup('template', 'switch-config.yml.j2') | from_yaml }}"

# Per-interface switch config.
switch_config_interfaces: "{{ lookup('template', 'switch-config-interfaces.yml.j2') | from_yaml }}"
